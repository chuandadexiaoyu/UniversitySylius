{% form_theme form 'SyliusWebBundle::forms.html.twig' %}

{{ form_errors(form) }}

<fieldset>
    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.name) }}
        </div>

    </div>
</fieldset>

<hr>
{{ form_widget(form._token) }}
